---
description: Create Prisma model
agent: backend-engineer
---

Create Prisma model with UUID as default ID (@default(uuid())), multi-tenant support (tenantId), timestamps (createdAt, updatedAt), relations to existing models, and indexes for performance.